#include <stdio.h>
#include <stdlib.h>

int main()
{
    int a=10,b=4,c=1,res;
    a++;
//    printf("El valor de a es: %d\n", a++);
//    printf("El valor de a es: %d\n", a);
//
//    res=a*3/5*4;
//    printf("res: %d",res);

    if (a==5) printf("No soc un 5.")
        else printf("Soc un 5."); // Cuando es sólo una orden se pueden quitar las llaves.

    return 0;
}
